{% extends 'base.html.twig' %}

{% block title %}Nouveau Véhicule{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Créer un nouveau véhicule</h1>

    <div class="card shadow-sm p-4">
        {{ include('vehicle/_form.html.twig', {'button_label': 'Créer', 'attr': {'class': 'form-group'}}) }}
    </div>

    <div class="mt-4">
        <a href="{{ path('app_vehicle_index') }}" class="btn btn-secondary">
            <i class="bi bi-arrow-left"></i> Retour à la liste
        </a>
    </div>
</div>
{% endblock %}
