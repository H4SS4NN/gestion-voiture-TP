{% extends 'base.html.twig' %}

{% block title %}Ajouter un commentaire{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Ajouter un commentaire pour : {{ vehicle.marque }} {{ vehicle.model }}</h1>

    <div class="card shadow-sm p-4">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button class="btn btn-primary mt-3" type="submit">Ajouter</button>
        {{ form_end(form) }}
    </div>

    <a href="{{ path('app_vehicle_show', {'id': vehicle.id}) }}" class="btn btn-secondary mt-3">Retour au véhicule</a>
</div>
{% endblock %}
